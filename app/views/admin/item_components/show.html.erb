<%= link_to 'Edit', edit_item_component_path(@item_component) %>
<%= link_to 'Delete', item_component_path(@item_component), data: {
  turbo_method: :delete,
  turbo_confirm: 'Are you sure?'
} %>
<% collections = %i[items] %>
<% columns = %i[name created_at updated_at] %>
<%= render partial: 'admin/shared/show', locals: { object: @item_component, title_column: :name, collections:, columns:  } %>