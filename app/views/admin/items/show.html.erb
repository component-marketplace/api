<%= link_to 'All Items', items_path %>
<% columns = %i[name price description] %>
<% collections = %i[item_components] %>
<%= render partial: 'admin/shared/show', locals: { object: @item, title_column: :name, collections:, columns:  } %>