<%= link_to 'All sale Receipts', sale_receipts_path %>
<%= link_to 'Edit', edit_sale_receipt_path(@sale_receipt) %>
<%= link_to 'Delete', sale_receipt_path(@sale_receipt), data: {
  turbo_method: :delete,
  turbo_confirm: 'Are you sure?'
} %>
<% associations = %i[item] %>
<% columns = %i[price quantity cost_to_build created_at updated_at] %>
<%= render partial: 'admin/shared/show', locals: { object: @sale_receipt, title_column: :id, associations:, columns: } %>