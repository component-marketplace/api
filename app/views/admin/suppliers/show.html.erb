<%= link_to 'Edit', edit_supplier_path(@supplier) %>
<%= link_to 'Delete', supplier_path(@supplier), data: {
  turbo_method: :delete,
  turbo_confirm: 'Are you sure?'
} %>
<% collections = %i[items item_components] %>
<% columns = %i[name created_at updated_at] %>
<%= render partial: 'admin/shared/show', locals: { object: @supplier, title_column: :name, collections:, columns:  } %>