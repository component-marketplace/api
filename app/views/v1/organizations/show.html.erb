<%= link_to 'All Organizations', organizations_path %>
<h1><%= @organization.name %></h1>
<%= link_to 'Edit', edit_organization_path(@organization) %>
<%= link_to 'Delete', organization_path(@organization), data: {
  turbo_method: :delete,
  turbo_confirm: 'Are you sure?'
} %>
